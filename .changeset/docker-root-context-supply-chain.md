---
"meteoswiss-mcp": patch
---

Security: the published Docker image is now built with every dependency override applied, and those overrides have been refreshed to close the advisories outstanding against the bundled transitive dependencies.

Dependencies only — no API, tool or behaviour change.
